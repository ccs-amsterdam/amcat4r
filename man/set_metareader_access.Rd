% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{set_metareader_access}
\alias{set_metareader_access}
\title{Set field-level access for metareaders}
\usage{
set_metareader_access(index, fields, credentials = NULL)
}
\arguments{
\item{index}{The index to modify fields for.}

\item{fields}{A named list of field access settings. Each element is named
after a field and contains a list with:
\itemize{
\item \code{access}: \code{"none"} (hidden), \code{"read"} (full), or \code{"snippet"} (truncated).
\item \code{max_snippet}: (optional, only used when \code{access = "snippet"}) a list
with \code{nomatch_chars} (chars to show without a query match, default 100),
\code{max_matches} (max highlighted matches, default 0), \code{match_chars}
(chars per match, default 50).
}}

\item{credentials}{The credentials to use. If not given, uses last login information.}
}
\description{
Controls which fields metareaders (unauthenticated/guest users) can access,
and how much of those fields they can see.
}
\examples{
\dontrun{
# Make 'url' fully readable and 'text' snippet-only for metareaders:
set_metareader_access("de-news", list(
  url  = list(access = "read"),
  text = list(access = "snippet", max_snippet = list(nomatch_chars = 150))
))
}
}
