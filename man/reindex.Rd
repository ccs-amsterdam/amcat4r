% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{reindex}
\alias{reindex}
\title{Reindex documents to a destination index}
\usage{
reindex(
  index,
  destination,
  fields = NULL,
  name = destination,
  description = NULL,
  guest_role = NULL,
  queries = NULL,
  filters = NULL,
  credentials = NULL
)
}
\arguments{
\item{index}{The source index name}

\item{destination}{The destination index name}

\item{fields}{Optional named list of per-field changes. Each element is a
named list with any of: \code{rename} (new field name), \code{exclude}
(logical, drop field), \code{type} (new AmCAT field type string).
E.g. \code{list(old_field=list(rename="new_field"), bad=list(exclude=TRUE),
  text_col=list(type="keyword"))}.}

\item{name}{Display name for the destination index (defaults to
\code{destination}); only used when creating a new index}

\item{description}{Optional description; only used when creating a new index}

\item{guest_role}{Optional guest role; only used when creating a new index}

\item{queries}{Optional list of query strings to filter documents during reindex}

\item{filters}{Optional list of filters to apply during reindex}

\item{credentials}{The credentials to use. If not given, uses last login information}
}
\description{
Reindexes documents from \code{index} to \code{destination}. If the
destination does not exist it is created. Field changes are specified via
\code{fields}: any field not mentioned is carried over from the source
unchanged.
}
